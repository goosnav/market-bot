[project]
name = "market-bot"
version = "0.1.0"
description = "Portable local-first outbound marketing workstation shell"
requires-python = ">=3.12"

[tool.market-bot]
sprint = "1"
stack_rationale = "zero-dependency sprint-1 runtime for portability and no hidden bootstrap"

